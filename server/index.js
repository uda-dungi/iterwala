// NOT USED — this project deploys to Vercel, which runs the /api folder directly as
// serverless functions (see api/checkout/initiate.ts and api/payu/callback.ts).
// This file was a standalone Express server for a Hostinger deploy that's no longer
// the plan. Left in place only because it can't be deleted from here; safe to ignore
// or remove by hand later. It has no dependency in package.json anymore, so don't run it.
export {};
