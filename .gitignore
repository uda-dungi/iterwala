# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

node_modules
dist
dist-ssr
*.local

# Local env files (keep .env.example tracked)
.env
.env.*
!.env.example

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Raw source photo exports (huge, only used locally to derive the compressed assets that
# actually get committed under src/assets/) — never belongs in git history.
src/assets/*.zip
